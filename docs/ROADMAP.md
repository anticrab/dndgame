# ROADMAP — направления развития и этапы

Документ задаёт **порядок** и **границы** этапов разработки. Каждый этап
определяется через входы (что должно быть готово), выходы (Definition of Done),
ключевые риски. Цель — никогда не делать «всё сразу»; продвигаемся вертикально:
каждый этап даёт **запускаемый** продукт.

Звёздочкой `*` помечены направления, заложенные сейчас «на будущее»: код пишем
так, чтобы они не требовали ломающих изменений, но реализацию откладываем.

---

## 0. Дизайн и планы (готово)

- `req.md` — внешние требования.
- `docs/DESIGN.md` — бизнес-логика по правилам 5e.
- `docs/ARCHITECTURE.md` — слои, паттерны, дерево каталогов.
- `docs/ADR/0001-hexagonal-architecture.md` — обоснование.

**DoD:** есть единый источник правды по правилам, слои и интерфейсы поименованы.

---

## 1. Ядро правил (domain core) — *в работе*

Иммутабельные value-объекты и базовые механики, без I/O.

- ✅ Кости (`DiceExpr`, `RollResult`, преимущество/помеха/крит).
- ✅ Характеристики (`Ability`, `AbilityScore`, `AbilityScores`, модификаторы).
- ✅ RNG-порт + Real/Scripted.
- 🔜 Хиты, временные хиты, кость хитов.
- 🔜 Типы урона, сопротивление/уязвимость/иммунитет.
- 🔜 Навыки, бонус мастерства, владения (оружие/доспех/инструмент/спасброски).
- 🔜 Состояния: базовый класс `Condition` + регистр; «ленивый набор» (Лежащий,
  Отравлен, Испуган, Парализован, Бессознательный, Невидимый).
- 🔜 Сущности: `Creature` (общий), `Character` (с уровнем/опытом),
  `Monster` (со статблоком и `MonsterAI`-стратегией).
- 🔜 `Spell`, `Item` (оружие, доспех, расходник, инструмент, магический).

**DoD:** ≥80% покрытия по domain тестами, `Creature` умеет получать урон,
лечиться, валиться в 0 хитов, делать спасбросок от смерти, накладывать/снимать
состояния. Логика чистая, без I/O.

**Риск:** перерастание domain в кашу. Контрмера — отдельные подпакеты
(`values/`, `entities/`, `rules/`, `conditions/`, `actions/`, `features/`).

---

## 2. Игровой движок и пошаговый бой

Единый `GameEngine` — центральный объект, оркеструющий тики и подсистемы
(см. `docs/ENGINE.md`).

- 🔜 `Battlefield` с **квадратной сеткой** (`(x, y)` int, Chebyshev-дистанция,
  8 соседей; см. `ADR/0002-square-grid.md`), позиционирование,
  движение, дистанции, провоцированные атаки, укрытие, террейн.
- 🔜 `Encounter` поверх Battlefield: инициатива, раунд/ход, очередь.
- 🔜 Действия как Command-объекты: Атака, Уклонение, Рывок, Отход, Помощь,
  Засада, Поиск, Магия (плейсхолдер), Использование.
- 🔜 Реакции: провоцированная атака.
- 🔜 Применение урона и спасброски по событиям.
- 🔜 Награда (XP, лут) по завершении.
- 🔜 Побег: отдельный режим выхода с карты.

**DoD:** можно прогнать сценарий «персонаж против гоблина» в скрипте — без UI,
по событиям; e2e-тест воспроизводим благодаря `ScriptedRNG`.

**Риск:** хочется впихнуть в движок UI. Контрмера — движок шлёт `Event`-ы,
UI слушает.

---

## 3. Создание персонажа и прогрессия

- 🔜 `CharacterBuilder` (Builder): класс → происхождение (вид+предыстория) →
  характеристики (3 стратегии) → мировоззрение → детали.
- 🔜 Расчёт КД/инициативы/пассивной Внимательности/ячеек заклинаний.
- 🔜 Прогрессия по уровням (1 → 3 для MVP), кость хитов, особенности класса.
- 🔜 «Быстрая» XP-кривая (см. `docs/PROGRESSION.md`), включая level-up
  посреди боя.
- 🔜 Инвентарь и экипировка с проверкой владений.

**DoD:** в коде можно создать PC из YAML-данных за ≤5 строк скрипта и попасть
в `Encounter`; повышение уровня меняет хиты/особенности корректно.

---

## 4. Хранение и сейвы

- 🔜 SQLite-схема: справочники контента (расы/классы/предметы/монстры/
  заклинания/предыстории/таблицы), таблица `saves`, таблица `settings`.
- 🔜 Миграции — простой кат-ап SQL-скриптов в `migrations/`.
- 🔜 Репозитории (`*Repository`) по требованию `req.md`.
- 🔜 `SaveService.save/load` — сериализация состояния (JSON в BLOB).

**DoD:** `dnd db init && dnd db seed` создаёт БД, можно сохранить/загрузить
состояние партии и продолжить с того же тика.

**Риск:** ORM-зуд. Не используем ORM — у нас простой sqlite3 + явный SQL,
учебная цель видеть Repository.

---

## 5. Контент-пайплайн (расширяемость данными)

- 🔜 YAML-схема (pydantic) для всех типов контента.
- 🔜 Загрузчик (`yaml_loader.py`) + сидер в SQLite.
- 🔜 Реестры (`FeatureRegistry`, `ConditionRegistry`, `ActionRegistry`,
  `MonsterAIRegistry`): YAML ссылается на имя плагина.
- 🔜 Базовый паковый контент: 2 вида, 2 класса, 3 предыстории, 8–12 предметов,
  6–10 монстров, 6 заклинаний.

**DoD:** добавление новой расы — 1 YAML-файл без правок кода ядра.

---

## 6. Сценарии мастера

- 🔜 YAML-сценарий со списком локаций, NPC, событий, диалогов, encounter-ов.
- 🔜 `ScenarioRuntime` — слушает `EventBus`, проигрывает триггеры
  `when–condition–effect`.
- 🔜 Загадки/проверки навыков/скрытые объекты.
- 🔜 Журнал квестов, флаги мира, целеуказание.
- 🔜 Демо-сценарий «Шахта Гнилого Дуба» (`docs/SCENARIO_DEMO.md`).

**DoD:** демо-сценарий проходится от начала до конца через CLI, флаги мира
влияют на финал, есть скрытая ветка с бонусом.

---

## 7. CLI ядро (Typer)

- 🔜 Команды: `dnd play`, `dnd character new/list/show`, `dnd db init/seed`,
  `dnd content list`.
- 🔜 Чистый CLI поверх `GameEngine` (можно играть без TUI — для скриптов и тестов).
- 🔜 Конфигурация (`~/.config/dnd/config.toml`).

**DoD:** в CLI можно создать PC, начать сценарий, играть текстом, сохраниться.

---

## 8. ASCII-рендеринг и TUI (Textual)

См. `docs/UI.md` (визуальный язык) и `docs/TUI.md` (архитектура
Textual-приложения, MVP-граница, многопоточная связка с движком).

### MVP (этап J) — играбельность на small-zoom

- 🔜 Каркас Textual-приложения, **2 темы** (`color` — по умолчанию,
  `monochrome` — для скриншотов и старых терминалов; см. `UI.md` §3).
- 🔜 Экран боя: карта (small-zoom 1×1), статус PC, очередь инициативы,
  лог (через `EventPrinter`), action-меню по горячим клавишам.
- 🔜 `TuiIntentProvider` — адаптер `PlayerIntentProvider` поверх
  очереди намерений + worker-thread `GameRunner.run`.
- 🔜 `dnd play --tui [--theme color|monochrome]`.
- 🔜 Headless e2e через `textual.pilot.Pilot` (CI-friendly).

### Пост-MVP (этапы K, L, далее)

- ✅ **Этап K — спрайт-система ASCII и редактор карт.** Реестр спрайтов
  (3 zoom-уровня), MapDocument + Repository, CLI `dnd sprite|map`, TUI
  editor (K7), 7-картный контент-набор (K8). См. `K9` аудит.
- ✅ **Этап L1 — inline-UX и viewport.** Удалили MovePicker/TargetPicker:
  вместо модалок BattleScreen переключается в inline-mode (NORMAL/MOVE/
  TARGET), курсор и highlights рисуются прямо на карте. Default
  zoom — `small` (1×1) для крупных деталей локаций; viewport с
  auto-follow держит PC в видимой области на больших картах
  (50×50 проверено в pilot smoke). См. `docs/TUI.md` §6.10.
- ✅ **Этап L2 — Ability framework.** Runtime-описание умения
  (`Ability` + `AbilityRegistry`), 6 default'ов (Attack/Dodge/Dash/
  Disengage/Interact/Break), `Creature.ability_ids` + `.keybindings`
  для per-creature override hotkey'ев, `ActionBarWidget` с динамической
  полосой `[a] Attack [d] Dodge …`. См. `docs/ABILITIES.md`.
- ✅ **Этап M — Move polish.** A*-pathfinder с обходом стен + учётом
  difficult terrain, chebyshev fast-path + cross-bias (пути ровнее),
  раскраска по budget speed_ft (green/yellow/red), hint
  `MOVE: (X,Y) cost=N/30 ft [нужен Dash]`, красная подсветка
  недостижимой клетки, ActionBarWidget скрыт (дублировал Footer).
- ✅ **Этап N — UX-polish боя.** HP/AC/дистанция целей в TARGET hint,
  HP-цвет в initiative + 'DEAD' маркер для убитых, цвета в StatusWidget
  (`act:[red]N[/]` после потраченного action), трупы (`%` red dim)
  остаются на карте и проходимы для pathfinder'а.
- ⏳ **Этап L3 — LocationArt + mouse support.** Детализированные
  крупные спрайты локаций (раз клетка 1×1, освобождаются пиксели для
  тайлов 5×3 / 7×5 с детализацией), интеграция мыши для panning/
  выбора цели. Отдельный спек после feedback'а пользователя по L1+L2.
- ✅ **Этап O — Инвентарь и снаряжение (частично).** `Item` /
  `ItemKind` / `ItemStack` / `Inventory` (slot+weight лимиты) +
  `Creature.inventory`; `ItemRepository` (YAML каталог);
  `parse_loot` для chest.contents (canonical+legacy формат);
  `PickupAction` + `PickupIntent` (free interaction, encumbrance
  guard); TUI `InventoryScreen` (hotkey 'l') и CLI пункт «Pickup».
  См. `docs/INVENTORY.md`.
  ⏳ Отложено в составе O / Q: **лут трупов** (Interact на `%` →
  loot menu — требует Corpse-entity, естественнее в Q),
  `Drop`/`Equip`/`Unequip`-intents (нужна связь `Creature.equipped_weapon`
  и `Item.id`), encumbrance penalties.
- ✅ **Этап Q — Death & dying.** Спасброски от смерти (PHB-2024 стр. 27):
  `Creature.uses_death_saves` + `death_saves`; dying/Unconscious при 0 HP;
  авто death-save на старте хода; авто-крит по лежачему в упор; massive
  damage → мгновенная смерть; `StabilizeAction` (Медицина DC10); end-condition
  (downed PC ≠ поражение, бой идёт пока есть спасаемые); трупы павших NPC —
  `CORPSE`-объект с лутом (переиспользует Interact+Pickup из O); события
  `DeathSaveRolled`/`CreatureStabilized`/`CreatureDied`; TUI death-save
  пипсы в StatusWidget. Закрыт O-хвост «лут трупов». См. `docs/DYING.md`.
  ⏳ Отложено: `Spare the Dying` (→ P), воскрешение, multi-PC стабилизация.
- ✅ **Этап P1 — Заклинания (фундамент).** Data-driven `Spell` (YAML) +
  `SpellRepository`; расширяемая обработка через `SpellEffectRegistry`
  (хендлер на тип воздействия — open/closed, новый тип = новый хендлер без
  правки `CastSpellAction`); 5 типов эффектов (ATTACK/SAVE/AUTO/HEAL/BUFF) +
  концентрация; 5 заклинаний (Fire Bolt, Sacred Flame, Magic Missile,
  Cure Wounds, Shield of Faith); `Creature` spellcasting-поля + spell
  attack/DC + упрощённые ячейки; `CastSpellIntent` + GameRunner;
  `ActionBarWidget` вернулся с динамическими `[1] Fire Bolt …` + спелл-
  таргетинг (враги/союзники); событие `SpellCast`; PC-кастер `mage_apprentice`
  в сценарии `mage_skirmish`. См. `docs/SPELLS.md`.
  - ✅ **P2 — AoE (зоны).** Конфигурируемый `TargetingSpec` (origin
    from_caster/at_point + shape circle/cone/line + размеры), чистая геометрия,
    `AreaShapeRegistry` (open/closed — новая форма = резолвер + регистрация),
    AoE-резолвинг в `CastSpellAction` (friendly fire), 3 заклинания
    (Fireball/Burning Hands/Lightning Bolt), TUI `BattleMode.AREA` (выбор точки
    для at-point / направления 8-сторон для from-caster + превью зоны). См.
    `docs/SPELLS.md`.
  - ⏳ **P2b** — мультитаргет (выбор N целей: Bless, распределение Magic Missile).
  - ⏳ **P3** — справка/inspect по заклинанию-способности → база знаний.
  - Отложено: upcasting, реакция-каст, классовые таблицы ячеек (→ R).
- 🔜 **Этап R — Уровни и опыт.** XP-tracking, level-up
  (HP, proficiency_bonus, ASI), TUI level-up screen (mockup C.5).
- 🔜 Главное меню, лист персонажа, экран исследования.
- 🔜 Анимация атаки (mockup C.2).

**DoD MVP J:** `dnd play --tui mvp_skirmish` реально проходится
клавиатурой до Victory / Defeat; обе темы работают; e2e через Pilot
зелёный; `import textual` не утекает за пределы `interfaces/tui/`.

**DoD общая (после K+L):** игра в TUI выглядит «как Bard's Tale /
Ultima», карта боя читаема на 80×24 (минимум), полнее на 120×40;
никаких всплывающих модалок при выборе цели/клетки; hotkey'и
переопределяются per-creature.

---

## 9. Расширяемость к multi-actor *

Сейчас один игрок управляет одним PC. По решению архитектора **порты под
будущие режимы не вводим заранее**, но пишем код так, чтобы каждое из
направлений добавилось без ломающих изменений.

Направления развития (порядок ориентировочный):

- **9a. Hot-seat** (один экран, несколько PC, передача управления, приватная
  панель мастера через PIN). Заложен в `UI.md` §8–9 и `ENGINE.md` §8.
- **9b. Live-мастер** — рядом с `AutoGameMaster` (`ScenarioRuntime`)
  появляется `LiveGameMaster` с собственным экраном и чит-консолью. См.
  `MASTER.md`.
- **9c. Сетевой режим** — host-сервер + клиенты. См. ниже §10.

**Что заложено сейчас (без портов):**

- 🔜 Все интенты (`PlayerCommand`) и события (`EngineEvent`) — pydantic-DTO,
  сериализуемые в JSON.
- 🔜 Раздельные типы `PlayerCommand` и `MasterIntent`; `GameEngine` имеет
  метод `apply_master_intent` (сейчас вызывается только из тестов).
- 🔜 `EventBus` — единственный канал событий движка наружу.
- 🔜 `RNG` и будущий `DiceRoller` — за портами; ни одно правило не вызывает
  `random.*` напрямую.
- 🔜 UI и БД — за портами. Domain не знает про них.

Этих инвариантов хватит, чтобы потом подключить hot-seat, мастера и сеть
за дни, а не месяцы. Любое нарушение инварианта блокирует MVP-PR.

**DoD MVP:** один локальный игрок проходит сценарий-демо; в коде нигде
не нарушены инварианты выше.

---

## 10. Сетевой режим (далёкое будущее) *

Описание здесь — на уровне «как не закрыть себе дверь». Реализация —
отдельный мажорный релиз после `1.0`.

- Транспорт — WebSocket или JSON-RPC поверх TCP, добавляется как
  адаптер слоя `infrastructure`.
- Host = `GameEngine` + сервер сокетов.
- Client = тонкий UI, слушает события, шлёт команды.
- Авторизация: PIN-коды игроков и мастера, как в hot-seat.

Это уже **не закладка** в коде; добавление будет своей серией PR.

---

## 11. Локализация (i18n)

См. `I18N.md`. Сжатый план:

- 🔜 Babel + gettext, `.po`/`.mo`-файлы.
- 🔜 Базовый язык — `en`. Русский — переключаемая локаль.
- 🔜 Контент в YAML — inline-словарь `{ en: "...", ru: "..." }` для
  всех локализуемых полей (`LocStr`).
- 🔜 UI-строки — через `_()`-обёртку; никаких конкатенаций.
- 🔜 Команды `make i18n-extract` / `make i18n-update LANG=ru` /
  `make i18n-compile`.

**DoD MVP:** UI работает на обоих языках, переключение в настройках,
сценарий-демо имеет полный bilingual-текст.

---

## 12. Качество

- 🔜 Покрытие ≥85% по domain/application.
- 🔜 e2e-тест: один полный прогон демо-сценария с `ScriptedRNG` и
  `RecordingUI`.
- 🔜 ruff/mypy в `strict`, pre-commit, CI (GitHub Actions / GitLab CI шаблон).
- 🔜 README с гифкой/скринкастом терминала.

---

## 13. Открытый мир (после MVP)

Когда сценарий-демо пройдёт стабильно — переходим к открытому миру:
- множественные сценарии-«мини-приключения», объединённые миром.
- глобальная карта (overland), путешествие между локациями.
- система квестов с возможностью отказаться/прервать.
- развитые диалоги с NPC, репутация фракций.
- генеративные случайные встречи.
- крафтинг, торговля.
- собственно — постепенный путь к **режиму «помощник за столом»**: когда
  приложение помогает живому мастеру и игрокам, а не ведёт их по рельсам.

Это не задачи MVP; они здесь, чтобы помнить вектор.

---

## Порядок прохода

```
1 ────────► 2 ─┬───► 3 ─┬───► 5 ─┬───► 6 ─┬───► 7 ─┬───► 8 ─┬───► 12
              │        │        │        │        │        │
              └───► 4 ─┘        │        │        │        │
                                │        │        │        │
                                └────────┴────────┴────────┘
                                          9, 11 (вшито в дизайн каждого слоя)
                                          10, 13 — после MVP
```

Поясняю порядок:
- Слой 1 нужен для всего. Без него правил нет.
- Слой 2 (движок + бой на квадратах) — сердцевина; ему нужен только domain.
- Слой 3 (создание персонажа) — нужны domain и контент-формат, можно
  параллельно с 4.
- Слой 4 (БД / сейвы) — независимый, параллельно с 3.
- 5 → 6 → 7 → 8 — стандартная последовательность вверх по стеку.
- 9 (multi-actor инварианты) и 11 (i18n) — не «отдельные этапы», а
  **контракты**, которые мы кладём в каждый слой сразу.
- 12 — параллельная инвестиция в каждом этапе.
- 10 (сеть) и 13 (открытый мир) — пост-MVP.

---

## Принципы прохождения

- **Каждый PR — вертикальный срез.** Никаких «отдельных PR на пустые классы».
- **Каждая новая фича сопровождается тестом по правилу из книги.** Маркер
  `pytest.mark.rules`.
- **Никаких «магических» решений.** Любое спорное место — отдельный ADR или
  пометка в `OPEN_QUESTIONS.md` до согласования.
- **MVP — это всё, что нужно для прохождения демо-сценария.** Всё остальное
  — пост-MVP.
