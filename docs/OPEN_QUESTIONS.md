# OPEN_QUESTIONS — принятые решения и оставшиеся вопросы

Все решения фиксируются здесь, а потом разъезжаются по соответствующим
design-документам. Когда вопрос закрыт — он остаётся в файле с пометкой
**ЗАКРЫТО** и кратким резюме, чтобы потом не возвращаться.

---

## ✅ Q1. Сетка боя — **квадраты**

Решение архитектора: **квадратная сетка**, 1 клетка = 5 футов. Стандарт
правил книги, проще и красивее в ASCII, лаконичнее в коде.

**Дополнительно:**
- Несколько существ могут стоять на одной клетке (нет жёсткого
  ограничения; в UI помечаем стоп-маркером «несколько»).
- Соседство по 8 направлениям (4 ортогональных + 4 диагональных).
- Дистанция по правилам книги: каждая клетка 5 футов, включая
  диагонали (Chebyshev). Если в будущем понадобится «1-2-1» правило
  старых редакций — это переключаемая стратегия.

**Затронуто:** `ENGINE.md` (раздел 2), `UI.md` (рендер карты).

---

## ✅ Q2. Размер боевой карты

Свободно задаётся сценарием. По умолчанию **10×8 клеток** для обычных
боёв, до **14×10** для боссов. UI выбирает спрайт-уровень исходя из
размера терминала и карты.

**Затронуто:** `ENGINE.md` §2, `UI.md` §2.

---

## ✅ Q3. 2 расы и 2 класса для MVP — **Человек + Эльф / Воин + Плут**

Воин — самый простой класс правилами. Плут даёт Скрытность, Скрытую атаку,
обнаружение ловушек — отлично подходит сценарию-демо (альтернативные пути,
скрытые комнаты). Волшебник — пост-MVP (заклинания дорого моделировать).

Подклассы (1-й подкласс на класс в MVP):
- Воин → **Чемпион** (Champion).
- Плут → **Вор** (Thief, без заклинаний — Мистический ловкач отложен).

**Затронуто:** `content/core/classes/` и `content/core/species/`.

---

## ✅ Q3.1. Предыстории MVP — **Солдат, Мудрец, Преступник**

Три предыстории, по одной на типовую характеристическую направленность:
Солдат (STR/CON), Мудрец (INT), Преступник (DEX/CHA). Покрывают типовые
билды Воина (Солдат) и Плута (Преступник), плюс универсальный Мудрец.

Каждая даёт: 2 навыка + 1 инструмент + черту + стартовое снаряжение, как
описано в `DESIGN.md` §6.

**Затронуто:** `content/core/backgrounds/*.yaml`.

---

## ✅ Q4. XP-кривая — **быстрая** (100/250/500/900)

Стандартная остаётся доступной через стратегию `XpCurve`; быстрая —
дефолт для сценариев с тегом `short`.

**Затронуто:** `PROGRESSION.md`.

---

## ✅ Q5. Сценарий-демо — **«Шахта Гнилого Дуба»**

6 локаций, 3 боя, 1 загадка, скрытая комната, альтернативный путь,
level-up в финале. См. `SCENARIO_DEMO.md`.

---

## ✅ Q6. Многопользовательский режим — **сейчас только hot-seat (заложить минимум)**

- **Сейчас:** один игрок, один персонаж.
- **Hot-seat** заложен в дизайне (передача управления между PC при их
  ходе, общий экран, возможность посмотреть лист товарища между ходами).
- **Live-мастер** и **сеть** — **не закладываем интерфейсы сейчас**.
  Принцип: пишем чистое ядро, чтобы потом эти направления добавились
  **без ломающих изменений**. Подробности в `MASTER.md` и
  `ROADMAP.md`.

**Конкретные инварианты, которые нельзя нарушать сейчас**, чтобы не
заблокировать будущие направления:
1. Все интенты и события — DTO (pydantic), сериализуемые в JSON.
2. `EventBus` — единственный канал событий движка наружу.
3. RNG — порт; ни одно правило не вызывает `random.*` напрямую.
4. UI и БД — за портами. Domain не знает про них.

**Затронуто:** `ROADMAP.md`, `ARCHITECTURE.md`, `MASTER.md`.

---

## ✅ Q7. Beginner-friendly послабления — **выключаем**

Чистые правила книги. Никаких «+2 HP на старте», «жирнее зелья» и т.п.
Сценарий-демо адаптируется под HP по правилам.

**Затронуто:** `PROGRESSION.md` (раздел вычищен).

---

## ✅ Q8. Минимальный размер терминала — **80×24**

При меньшем — отказ запуска TUI с подсказкой «увеличьте окно». CLI без
TUI работает и в меньшем (это для скриптов/тестов).

**Затронуто:** `UI.md`.

---

## ✅ Q9. Темы оформления — **две: «color» (цветная, по умолчанию) и «monochrome»**

«Color» — основная, красивая, ретро-палитра (тёмный фон, акценты по
семантике — союзник/враг/предмет/эффект). «Monochrome» — чёрно-белая
для скриншотов/принтскринов и старых терминалов. Все остальные темы
(phosphor/amber/vga-16) — на потом, добавятся как CSS-варианты при
желании, без правок кода.

**Затронуто:** `UI.md`.

---

## ✅ Q10. История партии — **в сейве полностью**

Полный лог событий и бросков пишется в сейв. Удалили сейв — потеряли
историю. Текущая сессия дополнительно держит лог в RAM (для replay и UI).

**Затронуто:** `ENGINE.md` (EventBus + GameLog), будущий `SaveService`.

---

## ✅ Q11. Куда писать БД и контент — **`platformdirs` + флаг `--workdir`**

- БД: `~/.local/share/dnd/dnd.sqlite`
- пользовательский контент: `~/.local/share/dnd/content/`
- конфиг: `~/.config/dnd/config.toml`
- логи: `~/.local/state/dnd/logs/`
- флаг `--workdir DIR` — перенести всё в `DIR/` (для разработки/учёбы).

---

## ✅ Q12. Изменение сценария при наличии сейва — **запрещено**

При попытке отредактировать/перезагрузить сценарий, по которому есть
активные сейвы: UI/CLI сообщает «по сценарию X есть N сейвов, удалите
их или сделайте копию сценария» (`dnd content clone scenario:X --as Y`).
Сейв хранит `(scenario_id, scenario_version)`; при несовпадении версии —
блок загрузки.

В будущем — система миграций сценариев (раздел в `ROADMAP.md` под
открытым миром).

**Затронуто:** будущий `ContentService`, `SaveService`.

---

## ✅ Q13. Раскладка прав при live-мастере (пост-MVP) — **движок бросает, мастер может перебросить с пометкой**

Все броски делает движок (`RNG`). Мастер может:
- запросить переброс конкретного броска (`reroll`);
- переопределить результат (`set_result`);
- наложить преимущество/помеху на следующий бросок;
- изменить HP/состояние/позицию без броска;
- спавнить/удалять NPC;
- стартовать/завершать encounter.

Каждое вмешательство пишется в `GameLog` с тегом `master_intervention`
и причиной (мастер указывает причину текстом). Это даёт честный лог
для постфактум-разборок.

**Затронуто:** `MASTER.md`.

---

## Решения по итогам правок и аудита

Архитектор фиксированно сказал «иди по рекомендациям». Решения ниже —
закрытые ✅, можно ссылаться.

### ✅ Q14. Локализация контента — **inline-словарь**

`name: { en: "Goblin", ru: "Гоблин" }` в YAML, реализуется через
`LocStr` pydantic-обёртку (`translations: dict[str, str]` — открыта на
третий язык). Файлы остаются человеко-читаемыми, нет дубликатов.

**Затронуто:** `I18N.md` §4, формат YAML контента.

### ✅ Q15. Статистика бросков — **текущая сессия + за всё время по сейву**

- Среднее по **сырому d20** (без бонусов и преимущества/помехи).
- Окна: **текущая сессия** + дополнительный показатель «**за всё время
  по этому сейву**».
- **Мини-гистограмма** по граням 1..20.
- Место: вкладка **Journal** на листе персонажа + быстрая клавиша **`S`**
  в бою для оверлея.

**Затронуто:** `UI.md` §7, будущий `DiceStatisticsService`
(`application/engine/dice_stats.py`).

### ✅ Q16. Защита панели мастера в hot-seat — **PIN-код**

PIN хранится в `SaveMeta.master_pin_hash` (sha-256 + salt). При входе в
`MasterScreen` (`Ctrl+M`) — диалог PIN. Неверно → возврат в обычный UI.
Это пост-MVP, заложено заранее.

**Затронуто:** `MASTER.md` §5.2, `SaveService` (хранение хэша).

### ✅ Q17. Ввод бросков с настоящих кубиков — **флаг сценария**

`dice_input: live | computer` в YAML сценария. По умолчанию `computer`.
Режим `live` поднимает `LiveDiceRoller`, который запрашивает значение
у игрока через `UserInterface.request_dice_input(ctx)`. С мастером —
мастер видит ввод и может перебросить.

**Затронуто:** `MASTER.md`, `ENGINE.md` §7.5 (`LiveDiceRoller` —
пост-MVP).

### ✅ Q18. Hot-seat hand-off — **кнопка передачи**

`handover_mode: confirm` по умолчанию: после хода активного PC экран
показывает «Pass control to {next_player} — press ENTER». Авто-режим
доступен опционально (`handover_mode: auto`).

**Затронуто:** `UI.md` §8.

---

## Решения по итогам аудита 02 (контракты)

### ✅ Q19. RNG как driven-port — **`domain/ports/rng.py`**

Гексагональная нотация: driven-порт (правила «дёргают наружу») живёт в
`domain/`. Это устраняет нарушение `domain → application` (A-001).
Реализации остаются в `infrastructure/rng/`. Будет оформлено отдельным
**ADR-0003**.

**Затронуто:** `src/dnd/domain/ports/rng.py` (новый), импорт в
`domain/values/dice.py`, импорт в `infrastructure/rng/*.py`. Cleanup в
коммите C.

### ✅ Q20. `DiceExpr.roll()` напрямую vs `DiceRoller` — **конвенция, без линтера**

Низкоуровневый `DiceExpr.roll(rng)` остаётся доступным как
internal-helper для:

- генерации stats при создании персонажа (`CharacterBuilder`);
- внутренних unit-тестов домена;
- внутренней реализации `ComputerDiceRoller`.

**Правила движка** (`attack_roll`, `save`, `ability_check`,
`damage_roll`) — **только** через `DiceRoller`. Это закреплено
документацией в `ENGINE.md` §7.1 и проверяется на код-ревью; линтер-
правило (например, через AST-проверку) — пост-MVP, если будут срывы.

### ✅ Q21. `UserInterface` — **один порт сейчас**

Не разбиваем на `InputPort`/`OutputPort`/`ViewPort` в MVP. Если порт
начнёт распухать сверх ~12 методов или появится подсистема, которой нужен
только подмножество (например, `BattleViewPort` для отдельного процесса
рендеринга боя) — будем расщеплять. Сейчас 7 методов — терпимо.

**Затронуто:** `ARCHITECTURE.md` §3.3, `UI.md` §10.

### ✅ Q22. `MasterIntent` — **Tagged Union на pydantic v2**

`Annotated[RerollIntent | SetHpIntent | ..., Field(discriminator="kind")]`.
Каждая операция — отдельный pydantic-класс с типизированными полями;
mypy/IDE/валидация — полноценные.

**Затронуто:** `ENGINE.md` §6.1, `MASTER.md` §3 (источник истины),
будущий `application/dto/master_intent.py`.

### ✅ Q23. Регистрация плагинов — **явная в composition root**

`Registry.register("rage", RageFeature)` в `composition.py`, **не**
декораторы при импорте. Причина — декораторы создают скрытое глобальное
состояние и порядок-импортов-зависимую инициализацию.

`ContentService.validate()` при старте проверяет, что все `id` из
YAML, ссылающиеся на плагины, имеют зарегистрированные классы. Не
зарегистрировано — явная ошибка с указанием конкретного `id`.

**Затронуто:** `ARCHITECTURE.md` §1, §3.12; будущий `composition.py`.

---

## Решения второй волны (перед стартом Creature)

### ✅ Q24. `Battlefield` — **mutable + `snapshot()`**

Карта боя — mutable-объект (методы `move_creature`, `set_terrain` меняют
его на месте). Для сохранения, replay и сетевого режима — метод
`snapshot() -> BattlefieldSnapshot` возвращает иммутабельный VO,
сериализуемый pydantic.

Альтернативы: чистый immutable (`with_creature_at(...)`) и event sourcing.
Первое — лишний копирование на каждом ходе; второе — overkill для
учебной партии 30 минут.

**Затронуто:** `domain/entities/battlefield.py`, `application/dto/snapshots.py`.

### ✅ Q25. `Character` vs `Creature` — **композиция**

`Character` содержит `creature: Creature` плюс свои поля (level, xp,
alignment, biography, spell_slots, hit_dice, death_saves). Доступ к
`Creature`-методам — через явное делегирование критичных операций
(`Character.take_damage`, `Character.apply_condition` и т.п.), не через
`__getattr__`. Прозрачнее для mypy strict, явнее в чтении.

Цена — больше boilerplate-проксей; цена приемлема.

**Затронуто:** `domain/entities/character.py`.

### ✅ Q26. `DeathSaveState` — **встроен в `Character` с самого начала**

Не отдельная сущность, не отдельный сервис: поле
`Character.death_saves: DeathSaveState` инициализируется при создании
PC. Состояние:

* `successes: int` (0..3)
* `failures: int` (0..3)
* `stable: bool` (если стабилизирован Медициной или нат-20)

При входе в 0 HP — `death_saves` начинает работать; при выходе
(лечение/нат-20/Медицина) — сбрасывается. См. `DESIGN.md` §2.5.

Реализуется сразу с Character, чтобы не возвращаться к этому при
первом TPK-сценарии.

**Затронуто:** `domain/entities/character.py`, новый раздел `DESIGN.md` §2.5.

### ✅ Q27. Concentration — **поле на `Creature` заложить сейчас**

В `Creature` будет поле `concentration: ConcentrationState | None`,
где `ConcentrationState` хранит `spell_id`, `applied_modifiers: list[ModifierId]`.

В MVP-классах (Воин/Плут) — заклинаний нет, поле всегда `None`. Но
**метод** `take_damage()` уже сейчас вызывает `_concentration_check()`
с `CON-save DC = max(10, damage // 2)`; реализация
`_concentration_check` пока no-op (только assert), переписывается в
один файл при добавлении первого заклинания концентрации.

Это «5-минутная заготовка», которая сэкономит час рефакторинга в
будущем.

**Затронуто:** `domain/entities/creature.py`.

---

## Решения третьей волны (заранее под расширяемость)

### ✅ Q28. Туман войны / LoS / освещение — **закладываем в движок с начала**

`Battlefield` отвечает за **геометрию** (стены, освещение клеток,
позиции существ). `VisibilitySystem` (отдельный сервис) отвечает за
вопросы «видит ли A B?» и «какие клетки видит партия?».

Подробности — в новом документе [`docs/VISIBILITY.md`](VISIBILITY.md).

В MVP: общий партийный туман войны (FoV-вычисляется как объединение
FoV всех PC). Per-character FoV — пост-MVP, но **интерфейс** уже
готов к этому.

**Затронуто:** `domain/entities/battlefield.py`, новый
`application/engine/visibility.py`, `VISIBILITY.md`.

### ✅ Q29. Targeting — **типизированная иерархия `Target`**

`Target` — discriminated union: `CreatureTarget`, `SquareTarget`,
`AreaTarget(shape, origin)`, `SelfTarget`, `MultiTarget(targets)`,
`AllInRange(filter)`.

`Action.target_kinds` — какие виды цели принимает действие.
`UI.request_target(action, ctx)` — спрашивает с подсветкой допустимых.

Подробности — в [`docs/TARGETING.md`](TARGETING.md).

**Затронуто:** `application/dto/target.py`, `domain/actions/base.py`,
`UI.md` §5.

### ✅ Q30. AI-паттерны NPC — **конфигурируемые в YAML**

`MonsterAI` — стратегия, выбираемая по `ai_profile: str` в YAML
монстра. Базовые профили: `brawler` (тупой ближайший+max урон),
`skirmisher` (ослабленных, тактичный), `ranged` (держит дистанцию),
`caster` (приоритет заклинаний), `support` (лечит союзников), `coward`
(убегает при HP<25%).

Каждый профиль — Python-класс, регистрируемый в `MonsterAIRegistry`.
Параметры профиля — данные YAML (например, `coward.flee_threshold: 0.25`).

Подробности — в новом [`docs/AI.md`](AI.md).

**Затронуто:** `application/ports/monster_ai.py`, `infrastructure/ai/`,
`AI.md`, контент-схема монстров.

### ✅ Q31. Action economy — **строгий протокол**

В `Encounter.run_turn()` существо имеет «бюджет» на ход:

* `action: ActionUsed | None`
* `bonus_action: ActionUsed | None`
* `reaction: ActionUsed | None` (живёт между ходами, сбрасывается на
  старте *своего* хода)
* `movement_remaining: int` (split — можно потратить часть до и часть
  после действия)
* `free_object_interaction: bool` (одно бесплатное взаимодействие в
  ход)

Это поля на `TurnBudget`-VO, в начале хода создаётся свежий, в конце
архивируется в `GameLog`. Action.execute проверяет наличие
соответствующего ресурса.

**Затронуто:** `domain/entities/turn_budget.py` (новый), `DESIGN.md`
§4 (расширенный).

### ✅ Q32. Multiattack — **через `Action.repeats`**

Воин-5 получает Extra Attack: `2 атаки за одно Action`. Гнолл и многие
NPC — то же. Реализуется как поле `AttackAction.repeats: int` (по
умолчанию 1). UI спрашивает цель для каждой атаки отдельно (или
«одну на всех», по решению игрока).

`Action.execute()` — внутри цикла по `repeats`, каждое попадание —
свой `AttackRoll` событие.

**Затронуто:** `domain/actions/attack.py`.

### ✅ Q33. Reactions — **отдельный реестр + opt-in от игрока**

`ReactionRegistry` объединён с `ActionRegistry` через поле
`category: ACTION | BONUS | REACTION` (закрывает аудитное I-020).

Когда триггер выпускает событие (например, `MovingOutOfReach`), движок
собирает кандидатов-реакций у каждого существа в зоне; если есть
реакция и игрок управляет существом — `UI.request_confirm("Use
opportunity attack?")` (с настройкой «всегда — yes/no»).

NPC — `MonsterAI.should_react(trigger, ctx) -> bool`.

**Затронуто:** `domain/actions/base.py`, `application/engine/reactions.py`
(новый), `MASTER.md` §3 (Master может включать/выключать реакции).

### ✅ Q34. Exhaustion — **отдельная под-система, не Condition**

Истощение — единственное накапливаемое состояние книги. Книга 2024
упростила: 6 уровней, каждый — -2 ко всем тестам/спасброскам/атакам;
уровень 6 — смерть. Не сводится к одной Condition.

Реализуется как поле `Creature.exhaustion: int` (0..6), не через
ConditionRegistry. Применение через специальные методы
`Creature.add_exhaustion()`, `Creature.remove_exhaustion()`. Влияет на
броски через автоматический `Modifier` источника
`source_kind=exhaustion`.

**Затронуто:** `domain/entities/creature.py`, `DESIGN.md` §5.

### ✅ Q35. `CreatureSize` + reach — **enum закладываем сейчас**

```python
class CreatureSize(StrEnum):
    TINY = "tiny"          # 2.5×2.5 фут (1/4 клетки, MVP не использует)
    SMALL = "small"        # 5×5 (1 клетка)
    MEDIUM = "medium"      # 5×5 (1 клетка) — стандарт MVP
    LARGE = "large"        # 10×10 (4 клетки) — пост-MVP
    HUGE = "huge"          # 15×15
    GARGANTUAN = "gargantuan"  # 20×20+
```

В MVP все существа `MEDIUM`. Но `Creature.size` есть с самого начала,
и `Battlefield` корректно работает с однокле́точными существами,
оставляя зазор под пост-MVP «один Creature занимает несколько клеток».

`Weapon.reach: int = 5` (в футах) для рукопашных; глефа = 10. Это
**уже сейчас** учитывается в attack-роуте через `range_to_target()`.

**Затронуто:** `domain/values/creature_size.py` (новый),
`domain/entities/creature.py`.

---

## Открытые вопросы от UI-агентов

В файлах `docs/ui_mockups/*.md` агенты по UX оставили ~15 точечных
открытых вопросов (например, цвет цифры в стеке существ, формат
диагональных стрелок пути на large-зуме, локализация кодов состояний).
Эти вопросы — **UX-косметика**, ни один не блокирует имплементацию.
Решим их по ходу реализации Textual-виджетов; ответы фиксируем
в `docs/ui_mockups/*.md` в строке «Открытые вопросы» каждого файла.

Если архитектор хочет — могу свести в отдельный раздел Q24…Q40 этого
файла. Пока оставляю в mockup-файлах для контекстной близости.
