"""Тестовые двойники (test doubles) для портов.

Здесь живут ручные моки, фейки и записывающие реализации:

* ``RecordingUI`` — реализация ``UserInterface``, которая записывает
  все вызовы для последующих ассерций в тестах.
* ``DictTranslator`` — фейковый ``Translator`` с явным dict-ом
  переводов.
* ``FrozenClock`` — фейковый ``Clock`` с управляемым временем.
* ``StubMonsterAI`` — фейковый ``MonsterAI``, всегда выдающий заранее
  заданный интент.
* ``InMemoryEventBus`` — *(опц.)* если потребуется отдельно от
  продуктовой реализации.

Сейчас директория пустая — двойники появятся вместе с соответствующими
портами и движком. Структура зарезервирована, чтобы импорты из тестов
сразу шли в правильное место: ``from tests._doubles.recording_ui import
RecordingUI``.

См. также:
- ``infrastructure/content/in_memory_repository.py`` — это **не тестовый
  дабл**, а полноценная реализация ``ContentRepository`` для CLI-режима
  «без БД» и для тестов (см. ARCHITECTURE.md §10).
"""
