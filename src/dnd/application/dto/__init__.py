"""DTO для пересечения границ слоёв (UI ↔ application ↔ infrastructure).

Все DTO здесь — pydantic-модели, сериализуемые в JSON. Эти типы
проходят через ``EventBus``, попадают в сейв (``GameLog``) и
теоретически могут уйти в сетевой транспорт без переделок.
"""
