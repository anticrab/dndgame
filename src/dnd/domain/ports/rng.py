"""Driven-port генератора случайных чисел.

Это **driven**-порт в терминологии гексагональной архитектуры: правила
домена «дёргают наружу» за случайностью, поэтому интерфейс принадлежит
домену. Расположение в ``domain/ports/`` зафиксировано в ADR-0003.

Весь домен бросает кости только через этот интерфейс. Это даёт:

* детерминированные тесты (см. ``infrastructure.rng.scripted_rng``);
* возможность отделить «реальный случай» от «крита по сценарию мастера»
  на уровне реализации (``ComputerDiceRoller`` vs ``LiveDiceRoller``).
"""

from __future__ import annotations

from typing import Protocol


class RNG(Protocol):
    """Минимальный контракт генератора случайных чисел.

    Единственный метод — бросок одного кубика с ``sides`` гранями. Любые
    более высокоуровневые операции (выбор случайного элемента, бросок с
    преимуществом, статистика) — выше по стеку: в ``DiceExpr.roll`` для
    механики костей и в ``DiceRoller`` для семантических бросков движка.
    """

    def roll(self, sides: int) -> int:
        """Бросить один кубик с ``sides`` гранями (k4, k6, k8, k10, k12, k20, k100).

        Должен возвращать равномерное целое в полуинтервале ``[1, sides]``.
        Реализации, не поддерживающие диапазон (``sides < 1``), должны
        бросать ``ValueError``.
        """
